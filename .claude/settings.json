{
  "permissions": {
    "allow": [
      "Bash(python -c ':*)",
      "Bash(curl -s http://127.0.0.1:8100/api/ai/roe)",
      "Bash(wc -l ai/*.py cop/server.py cop/static/app.js agents/*/*.py agents/*.py start.py)",
      "Bash(curl -s --max-time 3 http://localhost:8100/api/metrics)",
      "PowerShell($procs = Get-Process python,pythonw -ErrorAction SilentlyContinue; if \\($procs\\) { $procs | ForEach-Object { Stop-Process -Id $_.Id -Force -ErrorAction SilentlyContinue } ; Start-Sleep 2 }; $alive = Get-Process python,pythonw -ErrorAction SilentlyContinue; \"Killed. Alive python count: $\\($alive.Count\\)\")"
    ]
  }
}
